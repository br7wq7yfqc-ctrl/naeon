# generations/catalog.json — WIPED

**2026-08-15** · owner: kill the generations index, dump files only.

The S3 Index `s3://neon/generations/catalog.json` (601 classified rows, auto-bound by title/OCR) is **void**. Do not treat it as canon. Do not run `update_generations_catalog.py` / `build_catalog.py` to recreate it.

Raw plates stay as files:

- `generations/rendered/`
- `generations/imagine_images/`

New catalog is rebuilt **manually, one image at a time**.
