# Planet terrain edit (NMS-like, hard limits)

- Keys: `G` raise · `B` lower · `U` undo (12-deep)
- Cap: ±8 m/cell, 4000 m³ volume per planet, brush radius ~4.5 m
- Saves to `user://terrain_<id>.dat`
- FX: torus brush ring + soil dust particles
- Far sphere mesh unchanged — plate only near surface
