# Phase 0 playtest stitch (2026-08-14)

Working testing gate for the current prototype (not a closed Phase 0).
Smoke: `scripts/playtest_headless_smoke.sh` → OS_ERR=0 · TA_ERR=0 · MECH_CODE=0. No DMG.
OS-H ritual (headless steps, not FPS): `--playtest-ritual` or the mechanics gate. F5 loop: `docs/design/OPEN_SPACE_SC_BENCHMARK.md` § OS-H.

## Play
- Menu → Open Space or Aexion Clash
- GUI: `scripts/run_godot_playtest.sh` (gl_compatibility on software GL)
- OS-H F5: open `OpenSpace.tscn` → F5 → hold S (no pitch) → E land → F EVA → F board → Space takeoff → above atmo
- 10-min loop: fly/fight → land/lane → occupy/claim → harvest Contribution/Biomass

## Canon plates
Locked IDs: `godot/resources/canon_plates.json` (mirror of `docs/design/approved_sketches.json`).
Renders stay in `s3://neon/generations/canon/{class}/{id}/master.jpg` (not git).
If the local `generations/` tree or `user://canon_cache/{id}.jpg` exists, boards show the JPG; otherwise identity cards (faction / class / UUID).
Public GET on the bucket still needs `storage.admin` on SA `neon-access`.

## This pass
1. Arena runner + turret bolts: SoftScanCache enemies (no per-frame `get_nodes_in_group`)
2. CombatJuice floating numbers from a Label pool
3. AbilitySystem actually activates (was returning before `activate`)
4. Ability costs/CDs from rules/04; Firewall cleanses all 5 stacks
5. Hurtbox center/radius on dummy / player / turret
6. LayerContext.site_pin_id on Space + Arena + pad claim
7. PadAmbientLife / rover chassis quotes restored (parse blockers)
8. Infection HUD always 5 pips
9. Procedural Cybernex/gROT hero + dummy silhouettes when GLB missing (code-first, dual-theme)
10. Compact play HUD: HP/EN bars, ability chips, Infection 5 pips; F3 debug dump
11. Pulse bolts as oriented streaks; dummy death collapse (not instant hide)
12. Pad claim pylon (base/shaft/crown); ship cruise nacelles; OpenSpace HUD one-liner (F3 dump)
13. Clash arena perimeter + nexus halo (less greybox)
14. Honest land gate (no fake-land / auto-claim / secret brake); C claims after touchdown
15. HOVER altitude hold; NAV locked in dense atmosphere
16. 10-min loop: harvest-only economy_tick; objectives do not self-complete
17. Surface walker coyote/slope/landing; rover grip + Space brake
18. Station/ship interiors: honest atmo/life-support, sliding doors, E console, F seat only at ship seat
19. Pad contest: occupy-to-hold meter, decay on leave, turret guard, no harvest while contested, Hack/C pulses
20. HOVER ground-effect + pad-aware hold altitude
21. Interior pocket at y=9200 (was inside Nex-Prime r=1400); WorldRoot hidden; walker reparented so TPS camera stays visible
22. Atmospheric stall (vacuum=0, HOVER=0); NAV drops to SCM; HUD STALL line
23. Headless `--playtest-mechanics` gate in `scripts/playtest_headless_smoke.sh` (`timeout`, not `--quit-after` frames)
24. Interior HUD uses POCKET, not leftover ship AGL; occupy presence moves the contest meter
25. Pad contest guard actually fights: headless hitscan, pad_up aim, stays dead until 8s respawn; walker Pulse uses CamPivot
26. Harvest only while owner (or landed owning ship) is in the ring; EXTRACTING HUD line with rate + reserves
27. Seat→pilot no longer crashes HUD on a freed walker; headless playtest exits after PASS (dummy renderer hang)
28. Interior doors drop collision when the slab is open; cockpit E works when standing on the console
29. EVA tether warns at 80m and reels toward the ship past 120m (no death); HUD shows tether meters
30. Surface walker Nex-Firewall cleanses Infection (cap 5); pad V `soft_scan` is callable intel
31. Station recycler E vents POWER IDLE then restores POWER BUS; leftover console cooldown no longer blocks the next pocket
32. Scout hulls keep a CargoHold (no belly ramp); rover deploys on a landed pad and stores as a vehicle entry
33. Interior pocket HUD: GameHUD keeps HP/EN, infection, abilities, compact kind + I/F/E + life-support; radar/pads/CONTRIB/LAYER/briefing hidden; OpenSpace occupy one-liner off unless F3
34. Clash walker: planar accel/decel (not xz snap), coyote + jump buffer, 0.7s downed return to nexus spawn, ClashMatchDirector deaths
35. Pulse/dummy knock: CombatHits planar knock; dummy stagger skips AI so knock holds; lane holds fire after 0.22s windup
36. Clash lanes: K/D and pressure sync from AexionClash (0..100); random LaneBar drift removed
37. Rot Surge is a 4.5m AOE burst + knock (EE.ROT_SURGE / CD_SURGE); Cybernex kit unchanged
38. Interior doors: open hysteresis 0.55s, faster slide, emission + play_door; panels/lights follow recycler/atmo
39. Clash beacons start Neutral; C/Hack occupy-to-hold (1.75, decays on leave); full claim = objective + lane pressure
40. Extractor harvest only while owned by the player faction; ship bolts use ProjectilePool
41. Clash HUD drops SessionObjectives briefing / LAYER chip (F3 still dumps)
42. Mac 10-min soak still needs owner GPU (this VM is llvmpipe)
43. Cave crystal is a named `Crystal` node; V scan only inside the pocket; prompt at the deposit
44. Rover mouse look (yaw + cam pitch) while boarded
45. Clash HUD: MatchBanner legend ~4s then hide; ScoreLine `K D · OBJ · ECO`; LaneHUD lane+pressure only; GameHUD econ hidden (F3 dump)
46. GUI bolts sweep `CombatHits.apply_shot` along the step (no tunnel at ship speeds); shooter excluded
47. CombatHits considers `ship` hurtboxes; pad turrets track hulls; no planar knock on a landed ship
48. Hull critical: recover 35% HP, thrust cut 1.8s, shield hold 2.4s after hit, HP chrome shows hull+shields

49. Rover drive stays in `_physics_process`; mouse look stays in `_unhandled_input` (delta parse)
50. Clash lane towers are live Turrets (faction, 160 HP, collision); death = +28 soft pressure, not a planet flip
51. Ship afterburn: W+Shift spends 16 EN/s, thrust×1.55, speed×1.18; hull-crit and landed deny; Shift without W still descends
52. Walker + Clash jump: release Space while rising cuts upward speed (variable height)

## Audit pass 2026-08-15 (full code audit)
53. Arena Barrier uses a wall-sized collision shape (was the 60x1x60 floor shape — invisible slab, player spawned above the lanes)
54. `get_floor_angle(_up)` on walker + rover; sphere frames parallel-transport a tangent reference (no 90-degree snap near world +/-Z)
55. Turret cooldown drains the gated AI interval, so fire_rate is honest and FPS-independent; dead turrets/dummies leave the target groups
56. Pad contest holds transition 1.0 (a partial write flipped status to owned and killed decay); reserves regenerate while idle
57. One press = one claim pulse via `claim_pulse_from`; the ship must be landed; harvest credits the owning faction
58. `approach_assist` brakes on approach, not departure, and scales with delta
59. No-P2W: Knowledge no longer multiplies ability damage; War Score cap persists to user://; arena influence is capped + decaying
60. No friendly fire: hack, dummy fire and tower kills all check faction at the receiver
61. Infection amplifies gROT damage via `take_damage(amount, source_faction)`; Firewall decays and mitigates on the walker
62. Walker has a real downed window and `is_downed()`, so attackers disengage; EVA has a fuel budget and keeps planet gravity
63. Ship: HOVER hold only inside a gravity well, commanded-lift-only strip, launch stays in HOVER, one hardpoint per module type, recovery runs with no pilot
64. Clash: match_ended shows a result panel (Enter rematches), kills come from AexionClash, minimap orientation fixed, lane refill counts the living
65. Perf: shared arena meshes/materials, 10 Hz ownership visuals, one-time HUD styling, preloaded per-shot scripts, bolt pool counter reset on scene change
66. Arena Barrier moved out of the MID lane (it was a 2.5m wall lying along the lane that the player could stand on and fall off — the real "invisible ledge")
67. Friendly Clash towers moved behind the spawn camera (the MID tower at z=12 sat inside the chase camera and hid the player)
68. F3 overlay: GameHUD owns every stat readout; legacy TestArena labels off; the debug left column has a fixed stacked layout; pad radar clear of the vitals block
69. Match result panel is compact and top-centre so the fight stays visible

## Continuation pass 2026-08-15 (hardening to playable)
70. Pad streaming really unloads past 1.35x the build radius (was hide-only); measured flat at 502 nodes across two laps of all three planets, no planet left built
71. `Pads` joins the parked-node list, so far controllers stop polling input and scanning groups
72. Knock is built in the target ground plane (`up_direction`), not world XZ+Y — hits no longer shove a walker sideways on a sphere
73. Surface walker movement/sprint read the InputMap first (rebinding + gamepad now work on the main character)
74. `snap_to_surface` probes from just above the head and only reaches higher on a miss (was +40, which could strand you on a roof)
75. Perf: pooled jump FX, cached ship scan in EVA, ship Label3D written on change, `_recompute_stats` on attach/detach, attitude applied once per tick
76. Harvest VFX called; pad loss announced while you are away; siege denies unsupported hulls; F3 detail counter reads `queue_depth`
77. ENERGY_ECONOMY doc re-synced to the constants (it listed Pulse 6/0.55s against the real 18/5.0s)
78. Land gate is legible: `land_readiness_line()` on the ship HUD says which term still fails (`alt 600→120`, `pad 148m→90m`, `slow 12.3→12`, `LAND READY — E`); the refusal toast names the same term, and gate + readout share one `_land_envelope()`

## Galaxy layer G0 2026-08-15
79. `StarSystemCatalog` owns placement: star at the system origin, bodies on distinct orbits with distinct angles and inclinations (3800 / 7400 / 11800) instead of one hand-typed clump
80. Visible emissive star with corona; each planet takes its light direction from the star it orbits; the shadow light aims along the star-to-observer line
81. Asteroid belt is called for the first time and takes its band from the system layout
82. Gate anchors to ROT-Prime / Helios Reach / Echo Ruins authored but not spawned — a gate prop with no jump is the inert-system trap this branch was removing
83. Per-tier camera far clip sized to contain a system (LOW 22000 -> ULTRA 48000) with near 0.05 -> 0.25: at 8000 the two far bodies (10383 / 15960 from spawn) and every gate anchor were clipped away entirely
84. Star corona shell removed — a constant-alpha additive sphere rendered as a flat grey ring, not a glow; real corona needs rim falloff (manifest polish)
85. Ship module HP: engine/weapon/shield/cargo/extractor integrity scales function; hull stays; pad occupy repair (no cash). `--playtest-mechanics` asserts damaged engine reduces thrust. Power/cool/life buses: SE-A.
86. Zero-G EVA near ship (not on dirt): no planet gravity, fuel+tether stay, F reboard. Surface walker / OS-H ritual unchanged.
87. Pad fuel is a real SCM/HOVER tank: occupy-to-hold refill (no cash skip); Knowledge labels the pump only. Empty = no afterburn / limited thrust, not an OS-H hard lock. `--playtest-mechanics` asserts occupy→fuel-up.
88. Ship as place: I from seat → pocket → walk a real door/airlock → F seat (or I hatch → EVA). No fake locked doors. `--playtest-mechanics` asserts seat→pocket→seat.
89. Cargo dock on an occupied unnamed pad: land/occupy, transfer one crate pad↔`CargoHold` (ramp if present, no tractor). Knowledge labels the crate only — mass/value stay. `--playtest-mechanics` asserts occupy→one-unit.
90. Pad traffic: one CombatDummy guard + one visiting hull hold on an occupied unnamed pad. Knowledge labels only. `--playtest-mechanics` asserts pad traffic present.
91. OpenSpace play HUD: one left stack (fuel, cargo count, worst module, landed/occupy, EVA/0G). GFX/FPS/OBJECTIVE stay right/center; Clash HUD unchanged. Helper asserted in `--playtest-mechanics` / `--playtest-ritual`.
92. Occupied unnamed pad: live Contribution/Biomass on the OpenSpace HUD stack (ticks with harvest). Knowledge labels yield only. `--playtest-mechanics` asserts occupy→Contribution increased. Not ST-A.
93. Surface Pulse after EVA snap: grounded walker on Relief fires existing Pulse Bolt at a gROT CombatDummy near the pad. Knowledge labels only — Pulse DPS stays 11. `--playtest-mechanics` asserts EVA-snap→Pulse-hit.
94. Landing gear is a pad LAND gate: G toggles (SCM default up). SCM + gear up denies unnamed-pad land. HOVER near pad/surface auto-drops so OS-H E land still PASS. `--playtest-mechanics` asserts gear-down-before-land.
95. Occupied unnamed pad locker restocks player energy / Pulse CDs (occupy wait, no cash). Knowledge labels the locker only. Fuel + module repair stay. `--playtest-mechanics` asserts occupy→energy-up.
96. ST-B: visible extractor on unnamed pad + occupy → harvest → Contribution > 0 on HUD. Knowledge labels extractor / wallet only — never yield. `--playtest-mechanics` asserts occupy→harvest→Contribution.
98. ST-C: pad / NPC bench spends Contribution/Biomass and grants **one** catalog module. Cash-shop skip is impossible. Knowledge does not cheapen `rules/15`. `--playtest-mechanics` asserts spend → one module.
99. ST-D: hangar queue of **one** module on a catalog carrier hull. Refuse if mass/power exceeded. Not a mobile `SITE_*`. `--playtest-mechanics` prints hangar queue + mass/power refuse.
100. ST-E: player-owned orbital cluster of **two** catalog modules (dock + habitat) on Nex-Prime orbit. Not a city. Not `SITE_*`. Not `ORBITAL_STATIONS`. `--playtest-mechanics` prints two modules in one player orbital cluster.
101. ST-F: CX↔GR owner swap on **one** occupied unnamed pad. Theme + services list change. Harvest / print / hangar numbers stay. Not a second `SITE_*`. Not arena-flip. `--playtest-mechanics` prints CX↔GR flip + same-tier numbers.
102. ST-G: factory in the existing player cluster. Spend Contribution/Biomass at bench (c) → **one** catalog module. Without factory, (c) refuses. Cash-shop skip impossible. Knowledge does not cheapen `rules/15`. `--playtest-mechanics` prints factory present, spend, one module.
103. IN-A: station foyer/ops pocket from the player orbital cluster or an occupied unnamed pad; `hangar_bay` pocket on the ST-D catalog carrier. Neither is the ship cockpit. Doors are not locked props. Hatch returns to pad or dock (not MainMenu). `--playtest-mechanics` prints station ≠ ship, hangar_bay ≠ ship, hatch → pad/dock.
104. IN-C: `HangarBay` + `CargoHold` on the ST-D catalog carrier. `CargoRamp` deploys when landed / hover < 8 m AGL / docked and speed < 5; BLOCKED (toast, no deploy) when too fast or too high. Hatch from `hangar_bay` walks plates to the pad. No rover. `--playtest-mechanics` prints HangarBay+CargoHold, deploy, refuse, walk path hangar_bay → ramp → pad, no rover spawned.
105. AR-F: 3v3 local authority on the existing TestArena / Clash 60×60. Six actors (host + SoftNet visual bot puppets, or 3+3 isolated bots) on TOP/MID/BOT. Host process owns combat. G5 Clash-from-world stays closed. No new `SITE_*`. Knowledge does not change DPS. `--playtest-mechanics` prints 3v3 local match, 6 actors, local authority, G5 closed, no SITE_*.
106. AR-G: 5v5 local authority on the same TestArena / Clash 60×60 (TOP/MID/BOT + AR-D jungle). Ten actors (host Cybernex MID + nine SoftNet visual `CombatDummy` puppets, or 5+5 isolated). Host process owns combat. 3v3 stays startable (`--clash-3v3` / `start_isolated`). G5 stays closed. No new `SITE_*`. Knowledge does not change DPS. `--playtest-mechanics` prints 5v5 local match, 10 actors, local authority, G5 closed, no SITE_* — and keeps the AR-F 3v3 line.
134. AR-H: one door on an occupied unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) into Clash TestArena. F at the door. Not a city-map. Not G2. G5 cluster stays closed. Headless does not `change_scene`. AR-A…G stay. No leftover 5v5 soak. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print clash door F→Arena, TestArena target, not city-map, G5 closed, no SITE_*.
107. SE-A: live power / cool / life buses on the player hull. Fitted `ShipModule.power_draw` sums into power/cool. Overdraw / overheat = soft thrust/weapon sag (not a hard lock, not P2W). Hull LS matches IN-B (soft warn, no HP cut). HUD shows the three buses. Knowledge labels only. ST-D hangar mass/power refuse still holds. `--playtest-mechanics` prints power/cool/life values, overdraw sag, no P2W repair skip, IN-B LS still soft.
108. IN-F: second local viewer on the host sees catalog-carrier CargoRamp `DEPLOYED` plus a SoftNet visual rover puppet (or stored ghost). Host keeps IN-D/E rover authority. No second physical rover. No passenger seat. No `SITE_*`. `--playtest-mechanics` prints host rover authority, ramp DEPLOYED + rover puppet, no second physical rover, IN-E store/retrieve still works, no SITE_*, no second seat.
109. MC-A: one extra `CrewSeat` on the player ship pocket. F boards crew; I returns to `ship_int`. Pilot F still works. HUD `CREW n/2` is a label only — Knowledge does not change thrust / DPS / yield. Optional SoftNet visual puppet when a second local viewer exists. Host authority. No second hull. No passenger combat. No `SITE_*`. `--playtest-mechanics` prints crew seat F→crew I→same pocket, pilot still works, HUD CREW, no SITE_*.
120. MC-B: that crew seat is named `gunner` (SoftKnowledge / HUD `GUNNER`). HUD stays `CREW n/2` plus the role. Knowledge does not change Pulse / Hack / thrust / DPS / yield. F/I role stays `crew`. Pilot seat unchanged. Host keeps combat. No second hull. Not Clash. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print GUNNER label, Pulse/Hack numbers unchanged, no SITE_* — and keep the MC-A line.
127. MC-C: one extra `CrewSeat` (`EngineerSeat`) on the player ship pocket. Distinct `engineer_seat` offset. station_role `engineer` is SoftKnowledge / HUD `ENGINEER`. HUD `CREW n/3`. F boards crew; I returns to `ship_int`. Pilot F/I unchanged. MC-A gunner seat stays. Knowledge does not change thrust / DPS / yield / Pulse / Hack. Host authority. No second hull. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print ENGINEER label, CREW n/3, gunner stays, Pulse/Hack/thrust unchanged, no SITE_*.
131. MC-D: one extra `CrewSeat` (`ScannerSeat`) on the player ship pocket. Distinct `scanner_seat` offset. station_role `scanner` is SoftKnowledge / HUD `SCANNER`. HUD `CREW n/4`. F boards crew; I returns to `ship_int`. Pilot F/I unchanged. MC-A gunner + MC-C engineer stay. Knowledge does not change thrust / DPS / yield / Pulse / Hack. Host authority. No second hull. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print SCANNER label, CREW n/4, gunner+engineer stay, Pulse/Hack/thrust unchanged, no SITE_*.
128. KR-A: lifetime mastery derives **Knowledge Rank 0–4** (same `AllianceRanks` 0–4 family as BR-A). SoftKnowledge / HUD `KNOWLEDGE` / `KNOWLEDGE RANK` + rank number. BR-A Biomass Rank stays. Cybernex stays `CONTRIB`. Rank does not change harvest / Pulse / Hack / print / exclusive modules. Infection cap 5. No P2W. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print rank 0–4, HUD label, harvest/Pulse/Hack unchanged, BR-A still PASS, no SITE_*.
129. CR-A: lifetime Contribution wallet derives **Contribution Rank 0–4** (same `AllianceRanks` 0–4 family as BR-A). SoftKnowledge / HUD `CONTRIB` / `CONTRIBUTION` + rank number. gROT stays `BIOMASS` / BR-A. Rank does not change harvest / Pulse / Hack / print / exclusive modules. Infection cap 5. No P2W. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print rank 0–4, HUD label, harvest/Pulse/Hack unchanged, BR-A/KR-A still PASS, no SITE_*.
130. BT-D: one Cybernex animal-robot pack of 3 `CombatDummy` on an occupied unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) walks a tiny 3-state GDScript BT — gather, pulse-engage Pulse 11 both ways, scatter-return-to-pad. Host authority. Infection cap 5. No permadeath. Knowledge labels only. BT-A pad-guard, BT-B visitor, BT-C gROT swarm stay distinct. PV-A rival stays distinct. Not Clash waves. Not TestArena. Not AR leftover 5v5. G5 closed. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert gather / pulse-engage / scatter-return, Pulse 11 both ways, cap 5, BT-A/B/C stay, no SITE_*.
131. FL-A: first fleet seed — one extra allied pip on ST-A Strategy overlay (key B). Reuses the existing pad-visitor `NpcPilot` / NP-A hull. First extra pip stays. HUD `FLEET` is SoftKnowledge only. Click/select does not grant combat. Host keeps Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. BT-D Cybernex pack stays. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, first fleet pip visible, host authority, no SITE_*.
133. FL-B: second extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **3** (player + 2). HUD `FLEET n/3` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A first pip stays. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, second pip visible, host authority, FL-A still PASS, no SITE_*.
139. FL-C: third extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **4** (player + 3). HUD `FLEET n/4` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B first two extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, third pip visible, cap 4, FLEET 4/4, host authority, FL-A still PASS, FL-B still PASS, no SITE_*.
140. FL-D: fourth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **5** (player + 4). HUD `FLEET n/5` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C first three extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, fourth pip visible, cap 5, FLEET MANIFEST 5/5, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, no SITE_*.
143. FL-E: fifth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **6** (player + 5) toward 10–15. HUD `FLEET n/6` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D first four extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, fifth pip visible, cap 6, FLEET MANIFEST 6/6, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, no SITE_*.
144. FL-F: sixth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **7** (player + 6) toward 10–15. HUD `FLEET n/7` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E first five extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, sixth pip visible, cap 7, FLEET MANIFEST 7/7, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, no SITE_*.
146. FL-G: seventh extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **8** (player + 7) toward 10–15. HUD `FLEET n/8` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F first six extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, seventh pip visible, cap 8, FLEET MANIFEST 8/8, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, Infection cap 5, no SITE_*.
147. FL-H: eighth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **9** (player + 8) toward 10–15. HUD `FLEET n/9` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G first seven extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, eighth pip visible, cap 9, FLEET MANIFEST 9/9, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, Infection cap 5, no SITE_*.
148. FL-I: ninth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **10** (player + 9) toward 10–15. HUD `FLEET n/10` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H first eight extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, ninth pip visible, cap 10, FLEET MANIFEST 10/10, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, Infection cap 5, no SITE_*.
152. FL-J: tenth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **11** (player + 10) toward 10–15. HUD `FLEET n/11` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H / FL-I first nine extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, tenth pip visible, cap 11, FLEET MANIFEST 11/11, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, FL-I still PASS, Infection cap 5, no SITE_*.
154. FL-K: eleventh extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **12** (player + 11) toward 10–15. HUD `FLEET n/12` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H / FL-I / FL-J first ten extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, eleventh pip visible, cap 12, FLEET MANIFEST 12/12, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, FL-I still PASS, FL-J still PASS, Infection cap 5, no SITE_*.
155. FL-L: twelfth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **13** (player + 12) toward 10–15. HUD `FLEET n/13` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H / FL-I / FL-J / FL-K first eleven extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, twelfth pip visible, cap 13, FLEET MANIFEST 13/13, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, FL-I still PASS, FL-J still PASS, FL-K still PASS, Infection cap 5, no SITE_*.
157. FL-M: thirteenth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **14** (player + 13) toward 10–15. HUD `FLEET n/14` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 10–15 ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H / FL-I / FL-J / FL-K / FL-L first twelve extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, thirteenth pip visible, cap 14, FLEET MANIFEST 14/14, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, FL-I still PASS, FL-J still PASS, FL-K still PASS, FL-L still PASS, Infection cap 5, no SITE_*.
159. FL-N: fourteenth extra allied pip on the same ST-A overlay. SoftNet visual hull (pad-visitor / NP-A grammar). Cap **15** (player + 14) closes the 10–15 bar as SoftNet pips. HUD `FLEET n/15` is SoftKnowledge only. Click/select ≠ combat. Host Pulse / occupy. Overlay B still opens. Not 15 physical ships. Not a second OpenSpace. Not ENet. Not G5. FL-A / FL-B / FL-C / FL-D / FL-E / FL-F / FL-G / FL-H / FL-I / FL-J / FL-K / FL-L / FL-M first thirteen extra pips stay. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print overlay B opens, fourteenth pip visible, cap 15, FLEET MANIFEST 15/15, host authority, FL-A still PASS, FL-B still PASS, FL-C still PASS, FL-D still PASS, FL-E still PASS, FL-F still PASS, FL-G still PASS, FL-H still PASS, FL-I still PASS, FL-J still PASS, FL-K still PASS, FL-L still PASS, FL-M still PASS, Infection cap 5, no SITE_*.
132. SN-B: second local viewer seated with the host on the player hull / OpenSpace sees a SoftNet visual puppet of the host hull / pilot (or crew-seat pose). Host process owns Pulse / occupy / thrust. No second physical hull. SoftKnowledge `NET` label only. SN-A pad puppet stays. Clash unchanged. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print visual puppet present, no second physical hull, host authority, SN-A still PASS, no SITE_*, G5 closed — and keep the SN-A, MC-A/B/C, FL-A, BT-A/B/C/D, PV-A/B lines.
135. SN-C: second local viewer on ST-A Strategy overlay (key B) on an occupied unnamed pad sees a SoftNet visual puppet of the host pad / strategy actor (habitat / extractor / modules pose). Host process owns Pulse / occupy / Hack. No second physical pad modules. SoftKnowledge `NET` label only. SN-A and SN-B stay. AR-H pad door stays. Clash unchanged except that door. G5 cluster stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print visual puppet present, no second physical pad module, host authority, SN-A still PASS, SN-B still PASS, AR-H door still present, no SITE_*, G5 closed.
136. SN-D: second local viewer in Clash (`TestArena` / `ClashMatchDirector`) sees a SoftNet visual puppet of the host (arena / Clash pose). Host process owns Pulse / Hack / form. No second physical Clash dummy. SoftKnowledge `NET` label only. SN-A / SN-B / SN-C stay. AR-H pad door stays as the legal Clash entry. Headless does not `change_scene`. Not leftover 5v5 soak. G5 cluster stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print visual puppet present, no second physical Clash dummy, host authority, SN-A still PASS, SN-B still PASS, SN-C still PASS, AR-H door still present, no SITE_*, G5 closed.
137. DO-A: after occupy on **one** unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`), start/advance a contested Cybernex ↔ gROT transition (host authority). Uses existing `OwnershipData` / `OwnershipComponent` / `ContestedRing`. SoftKnowledge / HUD label only (`CONTESTED` / `CYBERNEX` / `GROT`). Knowledge ≠ DPS / yield / thrust / Pulse / Hack. Infection cap 5. ST-F owner swap stays. ST-A overlay B still opens. BT-A/B/C/D, SN-A/B/C/D, FL-A/B, AR-H, MC-A…D stay. Not leftover dirt-N / F-EVA / occupy HUD / pad-radar / OS-stack / hatch / HOVER soak. No `SITE_*`. No P2W. No G2–G6. `--playtest-mechanics` / `--playtest-ritual` print contested transition, SoftKnowledge labels, host authority, ST-F still PASS, overlay B opens, cap 5, no SITE_*.
138. DO-B: on the existing `PlayerOrbitalStation` cluster (ST-E dock + habitat; ST-G factory stays), host starts/advances a contested Cybernex ↔ gROT transition. Reuses `OwnershipData` / `OwnershipComponent` / `ContestedRing` from DO-A. SoftKnowledge / HUD label only (`CONTESTED` / `CYBERNEX` / `GROT`). Knowledge ≠ DPS / yield / thrust / Pulse / Hack / print. Infection cap 5. DO-A pad path stays. ST-F instant flip stays. ST-A overlay B still opens. Not a second pad. Not galaxy-wide. No `SITE_*`. No P2W. No G2–G6. `--playtest-mechanics` / `--playtest-ritual` print DO-B contested transition, SoftKnowledge labels, host authority, DO-A still PASS, ST-E/ST-G still PASS, ST-F still PASS, overlay B opens, cap 5, no SITE_*.
141. AR-I: CORE Turret HP → 0 ends the Clash match on both 3v3 and 5v5 local paths (`TestArena` / `ClashMatchDirector` / isolated `ClashLocalMatch`). SoftKnowledge HUD `WIN` / `LOSS` only (cosmetics / `TITLE` when daily WS cap 60 is hit). SoftSession records War Score +15 win / +3 loss. Knowledge ≠ DPS / Pulse / Hack / yield. Infection cap 5. Host authority. SN-D Clash SoftNet stays. AR-H door stays. `P0Slice.ORBITAL_STATIONS` stays false. Not leftover 5v5 soak. No `SITE_*`. No P2W. No G2–G6. `--playtest-mechanics` / `--playtest-ritual` / `--playtest-arena` print CORE→0 WIN/LOSS + WS grant with daily cap.
142. AR-J: one extra off-lane prime-class `ClashCamp` on the existing 60×60 (`TestArena` / `ClashMatchDirector`). Same ClashCamp / CombatDummy grammar as AR-D fangtooth. Soft contest announce. Drop = soft War Score only (not a unique weapon). SoftKnowledge / HUD labels camp / contest / WS. Knowledge ≠ DPS / yield / Pulse / Hack / exclusive modules. AR-A…AR-I stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print prime camp off-lane + soft WS.
143. AR-K: second session catalog option on the existing `ClashModuleBench` (`TestArena` / `ClashMatchDirector` 60×60). AR-E `ShipModule.SENSOR` stays; new option is catalog `CARGO` / Nex Hold (not a Paragon card, not a unique weapon). SoftKnowledge / HUD `HOLD` / `NEX HOLD` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. Session cost, never cash. AbilityKitCatalog prior 4 kits stay. AR-A…AR-J stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print session shop · 2 options · SoftKnowledge only.
145. AR-L: fifth Clash AbilityKit (`cx_lattice` / CX Lattice) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `LATTICE` / `NEX LATTICE` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 4 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-K stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print fifth kit · SoftKnowledge only.
147. AR-M: sixth Clash AbilityKit (`gr_vein` / GR Vein) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. gROT symmetric to CX Lattice. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `VEIN` / `ROT VEIN` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 5 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-L stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print sixth kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
149. AR-N: seventh Clash AbilityKit (`cx_prism` / CX Prism) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. Cybernex symmetric slot after CX Lattice / GR Vein. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `PRISM` / `NEX PRISM` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 6 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-M stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print seventh kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
151. AR-O: eighth Clash AbilityKit (`gr_facet` / GR Facet) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. gROT symmetric slot after CX Prism / GR Vein. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `FACET` / `ROT FACET` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 7 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-N stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print eighth kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
153. AR-P: ninth Clash AbilityKit (`cx_helix` / CX Helix) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. Cybernex symmetric slot after CX Prism / GR Facet. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `HELIX` / `NEX HELIX` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 8 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-O stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print ninth kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
154. AR-Q: tenth Clash AbilityKit (`gr_coil` / GR Coil) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60). gROT symmetric to CX Helix. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `COIL` / `ROT COIL` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 9 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-P stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6.
156. AR-R: eleventh Clash AbilityKit (`cx_spire` / CX Spire) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. Cybernex symmetric slot after GR Coil / CX Helix. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `SPIRE` / `NEX SPIRE` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 10 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-Q stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print eleventh kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
158. AR-S: twelfth Clash AbilityKit (`gr_thorn` / GR Thorn) on `AbilityKitCatalog` (`TestArena` / `ClashMatchDirector` 60×60) toward Phase-3 6–8 heroes. gROT symmetric to CX Spire. Same Pulse / utility / probe|surge / Form Cycle grammar from EnergyEconomy. SoftKnowledge / HUD `THORN` / `ROT THORN` only. Knowledge ≠ DPS / yield / Pulse / Hack / rank. No cash-shop / P2W / unique weapon. No Paragon card deck. Prior 11 kits stay. ClashModuleBench AR-E SENSOR + AR-K CARGO stay. AR-A…AR-R stay. Infection cap 5. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print twelfth kit · SoftKnowledge only · prior kits still PASS · Infection cap 5 · no SITE_*.
160. AR-T: first Clash minion-wave seed on the existing 60×60 (`TestArena` / `ClashMatchDirector` / `ClashWaves`). One host-authority lane wave of `CombatDummy`. SoftKnowledge / HUD `WAVE` / `MINION` only. Same Pulse 11 grammar as pad rivals. Infection cap 5. Not a unique weapon, XP power, cash-shop, or 13th AbilityKit. AbilityKitCatalog stays at 12 (`cx_nex`…`gr_thorn`). FL-N FLEET 15/15 stays. AR-A…AR-S stay. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print AR-T / WAVE · Pulse 11 · host · kits 12 · FLEET 15/15 · Infection cap 5 · no SITE_*.
161. AR-U: first Clash XP/leveling seed on the existing 60×60 (`TestArena` / `ClashMatchDirector` / `ClashLocalMatch`). SoftKnowledge / HUD `XP` / `LEVEL` only. Level is informational — never DPS / Pulse / yield / kit unlock / exclusive module. Rank / XP ≠ power. Infection cap 5. Not a 13th AbilityKit. Not another fleet pip. AbilityKitCatalog stays at 12 (`cx_nex`…`gr_thorn`). FL-N FLEET 15/15 stays. AR-T minion wave stays. AR-A…AR-T stay. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print AR-U / XP / LEVEL · Pulse 11 · host · kits 12 · FLEET 15/15 · Infection cap 5 · no SITE_*.
162. AR-V: second Clash lane minion-wave on the existing 60×60 (`TestArena` / `ClashMatchDirector` / `ClashWaves`). Mirror AR-T (host-authority `WAVE` / `MINION`, Pulse 11) on the opposite Clash lane. SoftKnowledge / HUD `WAVE` / `MINION` only. Infection cap 5. Not a 13th AbilityKit. Not XP power. Not another fleet pip. AbilityKitCatalog stays at 12 (`cx_nex`…`gr_thorn`). FL-N FLEET 15/15 stays. AR-T wave stays on its lane. AR-U XP/LEVEL stay informational. AR-A…AR-U stay. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print AR-V / second-lane WAVE · Pulse 11 · host · kits 12 · FLEET 15/15 · Infection cap 5 · no SITE_*.
163. AR-W: third Clash lane minion-wave on the existing 60×60 (`TestArena` / `ClashMatchDirector` / `ClashWaves`). Remaining Clash lane (MID) gets the same host-authority `WAVE` / `MINION` Pulse 11 seed. Completes the 3-lane MOBA seed. SoftKnowledge / HUD `WAVE` / `MINION` only. Infection cap 5. Not a 13th AbilityKit. Not XP power. Not another fleet pip. AbilityKitCatalog stays at 12 (`cx_nex`…`gr_thorn`). FL-N FLEET 15/15 stays. AR-T and AR-V waves stay. AR-U XP/LEVEL stay informational. AR-A…AR-V stay. Host authority. `P0Slice.ORBITAL_STATIONS` stays false. No `SITE_*`. No P2W. No G2–G6. `--playtest-arena` / `--playtest-mechanics` / `--playtest-ritual` print AR-W / third-lane WAVE · Pulse 11 · host · kits 12 · FLEET 15/15 · Infection cap 5 · no SITE_*.
121. SN-A: second local viewer on the occupied unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) sees a SoftNet visual `SurfaceWalker` puppet (optional PV-A rival pose). Host process owns Pulse / occupy. No second physical walker. Knowledge labels only. Clash unchanged. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` print visual puppet present, no second physical walker, host authority, no SITE_*, G5 closed — and keep the MC-A/B, PV-A, BT-A lines.
110. NP-G: after NP-B occupy/harvest the visitor NPC spends Contribution/Biomass at `PadPrintBench` §6(a) (same ST-C path) and gets **one** catalog module on an unnamed pad. Without spend → refuse. Cash-shop skip / `try_cash_repair_skip` stay false. Knowledge labels the bench only — does not cheapen `rules/15`. Not NP-C habitat, not factory (c), not hangar (b). No `SITE_*`. `--playtest-mechanics` asserts NPC spend → one catalog module; refuse without spend; no cash skip; no SITE_*.
111. NP-H: after NP-B occupy/harvest the visitor NPC queues **one** catalog module on a catalog carrier hangar (same ST-D `CarrierHangarQueue.enqueue_module`). Mass/power exceeded → refuse, no module queued. Knowledge labels the hangar only — does not change caps. Not a mobile `SITE_*`. Not factory (c). Not a second NP-C habitat. NP-G bench print stays. `--playtest-mechanics` / `--playtest-ritual` assert queue one module; mass/power refuse; no SITE_*.
112. NP-I: after NP-B occupy/harvest the visitor NPC spends Contribution/Biomass at the player-cluster factory bench (c) (same ST-G `print_one_factory_module`) and gets **one** catalog module. Without factory → refuse. Without spend → refuse. Cash-shop skip stays false. Knowledge labels the factory only — does not cheapen `rules/15`. Not hangar (b). Not pad bench (a). Not a second NP-C habitat. Not a mobile `SITE_*`. NP-G / NP-H stay. `--playtest-mechanics` / `--playtest-ritual` assert NPC spend → one catalog module; refuse without factory; no SITE_*.
113. Q-D: the existing pad visitor offers the **same** Q-A ContractBoard id (`occupy` / `harvest` / `deliver_crate` / `scan_extractor` on Nex-Prime). Player accepts from that NPC. Complete → SoftKnowledge `quest_intel` (`CONTRACT` → `PAD INTEL`). Harvest / print / hangar / Q-A / Q-B / Q-C numbers stay. No pay-to-complete. No Knowledge-gated exclusive modules. Not a second quest system. Not a campaign. `--playtest-mechanics` / `--playtest-ritual` assert NPC same board id; complete → quest_intel; numbers unchanged.
122. Q-E: the same ContractBoard can roll `scan_extractor`. Interact with the existing ST-B `PadHarvestExtractor` on an occupied unnamed pad. Complete reads extractor via SoftKnowledge → `quest_intel` (`CONTRACT` → `PAD INTEL`). Harvest / print / hangar numbers stay. No pay-to-complete. No exclusive modules. No `SITE_*`. Same `QA-*` id scheme. `--playtest-mechanics` / `--playtest-ritual` assert template present; complete → quest_intel; numbers unchanged; no SITE_*; Q-D still PASS.
114. HF-A: after EVA/occupy on an unnamed pad (same OpenSpace), gROT Hack applies **+1** Infection stack to a CombatDummy / pad-guard. Cap 5 stays; a 6th Hack refuses with `Infection cap 5`. Cybernex Firewall removes **1** stack (never below 0). Knowledge labels the stack only — DPS / yield / thrust stay. No unique weapon. No cash-shop cleanse. `--playtest-mechanics` / `--playtest-ritual` assert +1, cap refuse, Firewall −1, numbers unchanged.
115. HF-B: from the seated player hull (OpenSpace, not Clash), the same AbilitySystem Hack / Firewall hits a nearby pad-guard CombatDummy or visiting NpcPilot hull. +1 stack, cap 5 refuse (`Infection cap 5`), Firewall −1. Knowledge labels only — DPS / yield / thrust stay. TPS HF-A still PASS after EVA. No second ability system. No cash-shop cleanse. `--playtest-mechanics` / `--playtest-ritual` assert hull +1, cap refuse, Firewall −1, HF-A still PASS.
123. HF-C: while ST-A Strategy overlay is open (key B) on an occupied unnamed pad, the same AbilitySystem Hack / Firewall hits the pad-guard dummy or pad InfectionStatus. +1 stack, cap 5 refuse (`Infection cap 5`), Firewall −1. Knowledge labels only — DPS / yield / thrust stay. Esc/B leaves; ship + TPS stay. TPS HF-A and hull HF-B still PASS after leaving. No second ability system. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert overlay +1, cap refuse, Firewall −1, ST-A overlay still opens, no SITE_*, HF-A/B still PASS.
124. ST-H: occupy unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) → `BaseBuilder` places **one** `PadDefenseTurret` (catalog emplacement, not Clash `Turret.gd` / OUTER 160). HP present. Pulse DPS 11 at the PV-A rival or a BT-A-range hostile. Destroyed turret is not permadeath. ST-A habitat (0 combat) and ST-B extractor stay. Overlay B still opens. Knowledge labels only. No P2W repair. No new `SITE_*`. G5 stays closed. Clash / TestArena unchanged. `--playtest-mechanics` / `--playtest-ritual` assert turret present, HP, Pulse hit, no SITE_*, Clash TestArena unchanged, G5 closed — and keep ST-A…G / HF-C.
125. ST-I: occupy unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) → `BaseBuilder` places **one** `PadStorage` crate/hold. Holds **one** crate unit (same occupy dock unit as ship `CargoHold`). Transfer one crate pad-storage ↔ ship hold via the existing occupy dock path. Knowledge labels only — mass/value stay. ST-A habitat, ST-B extractor, ST-H turret stay. Overlay B still opens. Not a second ship `CargoHold`. No `SITE_*`. No P2W. `--playtest-mechanics` / `--playtest-ritual` assert storage present, one-unit transfer, no SITE_*, overlay B opens — and keep ST-A/B/H.
126. ST-J: occupy unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`) → `BaseBuilder` places **one** `PadHangarStub`. Not ST-D `CarrierHangarQueue` / IN-C `HangarBay`. Hatch/LAND stay on the pad (same `OpenSpace.tscn`, not MainMenu). Overlay B still opens. ST-A habitat, ST-B extractor, ST-H turret, ST-I storage stay. Knowledge labels only. No rover. No `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert hangar stub present, hatch→pad, LAND on pad, overlay B opens, no SITE_* — and keep ST-A/B/H/I.
116. PV-A: on an occupied unnamed pad (`Pad_North` / `Pad_Approach` / `Pad_Flank`), one host-authority rival `CombatDummy` (SoftNet visual) takes Pulse from the player walker and deals Pulse back. Pulse DPS stays 11. Win = rival HP → 0 (no permadeath). Infection cap 5 stays. Knowledge labels only — never DPS / HP. Clash HUD / TestArena unchanged. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert rival present, Pulse hit reduces rival HP, rival Pulse hits walker, G5 closed, no SITE_*.
117. PV-B: from the seated player hull on OpenSpace (not Clash), the same host-authority pad rival takes Pulse and deals Pulse back. Pulse DPS stays 11. Win = rival HP → 0 (no permadeath). Infection cap 5 stays. Knowledge labels only — never DPS / HP. TPS PV-A still PASS after EVA. SoftNet visual bind only. Clash HUD / TestArena unchanged. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert seated hull Pulse hit reduces rival HP, rival Pulse hits hull, win HP 0, G5 closed, no SITE_*.
118. BT-A: one occupied unnamed-pad guard (`PadGuardDummy`) walks a tiny 3-state GDScript BehaviorTree — patrol near the plate, engage the player `SurfaceWalker` with Pulse, return-to-pad when the walker leaves. Pulse DPS stays 11. Host authority. Knowledge labels only. PV-A rival stays a distinct actor. Not Clash waves. Not 10k CCU. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert patrol state, engage Pulse hit, return-to-pad, G5 closed, no SITE_*.
119. BT-B: one visitor `NpcPilot` on that occupied unnamed pad walks a sibling 3-state GDScript BehaviorTree — approach the plate, hold (occupy/harvest still legal; NP-B numbers stay), leave. Pulse DPS stays 11. Host authority. Pad-guard BT-A stays. Knowledge labels only. Not Clash waves. Not 10k CCU. G5 stays closed. No new `SITE_*`. `--playtest-mechanics` / `--playtest-ritual` assert approach, hold harvest, leave; BT-A still PASS; G5 closed; no SITE_*.
96. OS-H descend empties the SCM/HOVER tank. LAND+occupy on an unnamed pad restores fuel (occupy wait, no cash). Seed/transition do not gate the pump. `--playtest-ritual` / `--playtest-mechanics` print `occupy fuel after LAND` and fail if still 0.
97. OS-H leftover: after pad LAND the walker snaps onto the plate (not dirt void / fall-through). Takeoff returns to HOVER/space in the same `OpenSpace.tscn` (no MainMenu reload). `--playtest-ritual` notes `walker on pad, not void` and `no MainMenu / load screen`.
